# Repository workflow

- `master` must remain at the initial commit containing the original v4 file.
- Do not commit, merge, rebase, reset, or otherwise move `master` beyond that initial commit.
- Perform all subsequent work on task branches. Do not merge task branches into `master`.
- Keep commits small and grouped by meaningful changes; include relevant tests with implementation changes.
